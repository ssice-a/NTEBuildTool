## Agent skills

### Issue tracker

本仓库使用本地 Markdown 作为 issue tracker，问题、PRD 和拆分任务写入 `.scratch/`。详见 `docs/agents/issue-tracker.md`。

### Triage labels

本仓库使用默认五段 triage 状态：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库采用 single-context 文档布局：根目录 `CONTEXT.md` 描述领域语言，`docs/adr/` 记录架构决策。详见 `docs/agents/domain.md`。
